# Leave empty for same-origin relative /api requests (recommended — works on
# localhost and any server/domain without rebuilding). Only set this if the
# backend is served from a different domain than the frontend.
VITE_API_URL= PutYourURLHere
